# Empty = use same-origin relative URLs; the CRA dev-server proxy (package.json
# "proxy") forwards API calls to the backend server-side, so only port 3000 needs
# to be tunneled. Set this back to http://localhost:5001 if not using the proxy.
REACT_APP_API_HOST=